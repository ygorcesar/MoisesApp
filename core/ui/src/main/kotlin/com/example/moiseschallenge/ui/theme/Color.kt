package com.example.moiseschallenge.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors
val MoisesPrimary = Color(0xFFFFFFFF)
val MoisesSecondary = Color(0xFF8E8E93)

// Background colors
val MoisesBackground = Color(0xFF000000)
val MoisesSurface = Color(0xFF1C1C1E)
val MoisesSurfaceVariant = Color(0xFF2C2C2E)

// Text colors
val MoisesOnBackground = Color(0xFFFFFFFF)
val MoisesOnSurface = Color(0xFFFFFFFF)
val MoisesOnSurfaceVariant = Color(0xFF8E8E93)

// Accent colors
val MoisesAccent = Color(0xFFFFFFFF)
val MoisesError = Color(0xFFCF6679)

// Player colors
val PlayerProgressTrack = Color(0xFF3A3A3A)
val PlayerProgressIndicator = Color(0xFFFFFFFF)

// Splash gradient colors
val SplashGradientStart = Color(0xFF000000)
val SplashGradientEnd = Color(0xFF01323B)

val SplashGradientColorStops = arrayOf(
    0.0f to SplashGradientStart,
    0.2f to SplashGradientStart,
    1.0f to SplashGradientEnd
)
